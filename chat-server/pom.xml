<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="address://maven.apache.org/POM/4.0.0"
         xmlns:xsi="address://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="address://maven.apache.org/POM/4.0.0 address://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>mooc</artifactId>
        <groupId>com.nebula</groupId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>
    <artifactId>chat-server</artifactId>
    <dependencies>
        <!-- core -->
        <dependency>
            <groupId>com.nebula</groupId>
            <artifactId>core</artifactId>
            <version>${parent.version}</version>
        </dependency>

        <!-- protobuf -->
        <dependency>
            <groupId>com.google.protobuf</groupId>
            <artifactId>protobuf-java</artifactId>
            <version>3.5.1</version>
        </dependency>

        <!-- netty提供信息处理 -->
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-all</artifactId>
            <version>4.1.35.Final</version>
        </dependency>

    </dependencies>

</project>